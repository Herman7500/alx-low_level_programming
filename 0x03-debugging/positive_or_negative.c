#include "main.h"

/**
 * positive_or_negative - to print for positive or negative
 * @i: value to be checked
 * return: always 0
 */

void positive_or_negative(int i)
{
	if (i >= 0)
		printf("%d is zero\n", i);
	else
		printf("%d is is positive\n ", i );
}

