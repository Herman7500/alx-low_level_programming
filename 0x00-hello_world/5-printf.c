#include <stdio.h>
/* this will enable us to;
 * write this without having to face some challenges.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
		return (0);
}
